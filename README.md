This is a caching proxy server written in C++ that uses httplib.h.
It uses an LRU cache with a "capacity" argument in order to control the size of the cache.
My motivation for the project idea can be found here: https://roadmap.sh/projects/caching-server.